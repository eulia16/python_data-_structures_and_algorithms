#same as R1.4 but only uses single command and uses list comprehension and python's built in sum function

#this used list comprension and the sum function to obtain the same result as
#the previous fucntion(R1.4.py)
summation = sum([k**2 for k in range(0, 5)])

print(sum([k**2 for k in range(0, 5)]))
